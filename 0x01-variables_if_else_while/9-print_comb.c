#include <stdio.h>

/**
 * main - prints all possible combination of single-digit number
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		puttchar(n)
			if (n != 57)
			{
				putchat(',');
				putchar(' ');
			}
	}
	putchar('\n');
	return (0);
}
